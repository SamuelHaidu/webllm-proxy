"""Swappable strategies for recurring workaround problems -- today just
`tool_calling` (how to recover structured tool calls from a provider that
has no native function-calling API). Each strategy implements the matching
port in `domain.ports`."""
