"""Provider-agnostic transport: the browser worker (`browser.py`) and
cross-platform process/lock hygiene for the profile-scoped Chrome it drives
(`process.py`). Depends on `domain` (ports) and `infra` only."""
